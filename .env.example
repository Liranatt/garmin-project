# =============================================================
# Garmin Health Tracker - Environment Variables
# =============================================================
# Copy this file to .env and fill in your values:
#   cp .env.example .env
# =============================================================

# --- Garmin Connect Credentials ---
GARMIN_EMAIL=your_email@example.com
GARMIN_PASSWORD=your_password

# --- PostgreSQL Connection (Heroku) ---
POSTGRES_USER=your_db_user
POSTGRES_PASSWORD=your_db_password
POSTGRES_HOST=your_host.amazonaws.com
POSTGRES_PORT=5432
POSTGRES_DB=your_db_name
POSTGRES_CONNECTION_STRING=postgresql://user:password@host:5432/dbname

# --- Google Gemini (for CrewAI agents) ---
# Get your key at: https://aistudio.google.com/apikey
GOOGLE_API_KEY=your_gemini_api_key

# --- Garth Session Directory (optional) ---
# Where garth stores OAuth tokens for session reuse.
# Defaults to ~/.garth if not set.
GARTH_HOME=~/.garth

# --- Garmin Bulk-Download DB (optional, for garmin_merge.py only) ---
# GARMIN_CONNECTION_STRING=postgresql://user:password@localhost:5432/garmin
